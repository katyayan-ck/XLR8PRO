# Open bugs index (generated by `php artisan ai:refresh-context` — do not edit)

Full entries: grep `### BUG-NNN` in `docs/refactor/known-bugs-report.md`. Generated 27-09-2026 01:49.

| Bug | Severity | Status | Title |
|---|---|---|---|
| BUG-008 | High | MITIGATED (DEC-037) | `EmployeeCrudController` references non-existent columns (`person_id` etc.) |
| BUG-009 | High | MITIGATED (DEC-037/038) | `BrandCrudController` — `xlr8_vehicle_brand` table doesn't exist |
| BUG-019 | Unknown | OPEN — needs owner input | Unexplained large external change to `BookingCrudController.php` |
| BUG-020 | High | MITIGATED (DEC-037) | `PersonAddressCrudController` references columns that don't exist (`type`, `person_id`, `is_primary`) |
| BUG-021 | Critical | MITIGATED (DEC-037) | `PersonBankingDetailCrudController` — missing `CrudTrait` + wrong column names (stacked) |
| BUG-045 | Low | OPEN (documented only) | `app/Http/Controllers/Admin/oldEnquiryCrudController.php` — entirely unreachable, no route anywhere references it (superseded draft of the real `EnquiryCrudCon… |
| BUG-056 | Low | OPEN | Admin menu's "Approved Quotations" link points at a route/feature that has never existed (pre-existing, unrelated to URL rename) |
| BUG-061 | Medium | OPEN | `BookingCrudController::pendingKyc()`/`stockReport()`/`intInFinance()`/`otfProcess()` fatal (500) with real data — pre-existing, unrelated to this session's ch… |
| BUG-062 | Low | OPEN | 5 Booking menu links (dummy, ready-to-invoice, pending-incomplete-votfs, rto-agent-tracker, brokerage) point at URLs with no route ever registered |
| BUG-069 | Low | MITIGATED (DEC-038) | Admin menu's "Price List" link (`backpack_url('pricing')`) targets a URL with no registered route — always 404's; also its visibility condition previously used… |
| BUG-028 | Low | OPEN | `FinanceCrudController` + `InsuranceCrudController` both declare List/Create/Update/Delete Operation traits + a `ScopedCrud` trait, but neither has a `setup()`… |
| BUG-029 | High | OPEN | `RtoCrudController::import()` references `$spreadsheetId`, which is never defined anywhere in the file/class — the RTO import feature has likely never worked a… |
| BUG-030 | Critical | OPEN | `App\Helpers\XCommonHelper` references 8+ model classes (`X_Location`, `X_Segment`, `X_Branch`, `X_Department`, `X_Division`, `X_Designation`, `X_Vertical`, `X… |
| BUG-031 | Critical | OPEN | `SpareRequestCrudController`'s own `index()` screen fatals unconditionally (`RouteNotFoundException`) because `list.blade.php` calls `route('spare-request.data… |
| BUG-032 | Critical | OPEN | `SpareRequestCrudController::setup()` never calls `CRUD::setModel()` — `store()`/`update()`/`destroy()` (the unmodified trait defaults) fatal on a null model; … |
| BUG-033 | Medium | OPEN | `routes/backpack/booking.php` registers `DELETE accounts/receipt/{id}` → `[ReceiptCrudController::class, 'destroy']`, but `ReceiptCrudController` has no `destr… |
| BUG-083 | Medium | OPEN (pre-existing, carried forward — n… | `BranchCrudController`'s own `setupListOperation()` override shadows `ScopedCrud` trait's data-scoping logic — branch data-scoping is silently never applied |
| BUG-085 | Low | OPEN (environmental — scoped runs with … | `vendor/bin/phpstan analyse` (Larastan) OOMs on a full-project run in this dev environment (Windows paging file too small) |
| BUG-090 | Medium | MITIGATED (DEC-043: 34 role-less users … | 36 employees have `designation_code`/`desig_code` values that don't exist in `xlr8_admin_designation`; 30 have an empty `primary_branch_code` — real data-quali… |
| BUG-092 | Medium | OPEN (documented only — pre-existing da… | Booking list's Segment/Model/Variant/Color columns are sourced only via a join to `xlr8_crm_enquiries` on `bookings.enq_no`; 42/43 existing bookings have an em… |
| BUG-095 | Medium | OPEN (documented only — looks intention… | 3 hardcoded user-ID whitelists (`[5, 23, 123]`, one also adds `$user->id`) gate Order Verification / Pending DMS action buttons in `BookingCrudController.php`,… |
| BUG-097 | Medium | OPEN (documented only — needs a design … | VOTF generation (`generateVotfNumber`) and persistence (`otfSave`) are two separate HTTP requests with no shared lock/reservation — a preview endpoint computes… |
| BUG-101 | Low | OPEN (documented only — long-standing d… | `dmsupdate()`'s "BEV/Personal segment → order 3 when DMS SO missing" branch is dead code — `xlr8_booking_master` has no `segment_code` column, so a freshly-loa… |
| BUG-102 | Low | OPEN (documented only — data isn't lost… | `exchangeUpdate()`'s `XExchange` upsert payload sets 9 fields (`enum_master1/2`, `vehicle_details/2`, `registration_no`, `manufacturing_year`, `odometer_readin… |
| BUG-106 | Low | OPEN (documented only — found incidenta… | Route `sales.booking.delivered-view` (`GET sales/booking/delivered-view/{id}`) points to `BookingCrudController::deliveredView()`, a method that doesn't exist … |
| BUG-107 | Low | OPEN (documented only — a 3rd instance … | `resources/views/admin/booking/edit.blade.php` is completely unreferenced by any route or controller method — `BookingCrudController::edit()` delegates to Back… |
| BUG-109 | Low | OPEN (documented only — found incidenta… | `resources/views/admin/booking/pending-delivery.blade.php` (singular) is unreferenced by any `view()` call in the controller — a 4th orphaned Booking view foun… |
| BUG-111 | Low | OPEN (documented only — found while sca… | `app/Http/Controllers/Admin/oldEnquiryCrudController.php` is a full, unrouted duplicate of `EnquiryCrudController` (same class name, wrong namespace `App\Http\… |
| BUG-116 | High | OPEN (documented only — genuinely unimp… | Spares module's create/edit forms call 3 AJAX endpoints that don't exist as routes at all: `admin/fetch-parts` (controller method `fetchParts()` exists but has… |
| BUG-119 | High (Booking `export-legacy`'s Enquiry equivalent is used by the Export button on 7 live Enquiry screens; Enquiry's `erroneous` is a real sidebar menu link) | **13 of 18 FIXED** (11 Booking + 4 Enqu… | 15 registered Booking routes (`delivered`, `deliveredList`, `deliveredView`, `erroneousEntries`, `erroneousEntriesData`, `finRetailed`, `invoicedList`, `liveOr… |
| BUG-122 | High | OPEN (documented only — needs the actua… | 5 Booking listing screens (`reports/branch-booking`, `reports/consolidated-booking`, `reports/live-order`, `reports/pending-actions`, `reports/stock`) 500 beca… |
| BUG-139 | Medium (unreachable today) | PARTIALLY FIXED — (1) fixed; (2)/(3) OP… | `DocService`: `search()` return type `Collection` is unimported (resolves to nonexistent `App\Services\Collection`); `approve()` calls `ApprovalService::approv… |
| BUG-153 | Low | OPEN (needs business rule) | `getChassisNumbers` filters on `status = 'available'`, which no stock row has |
| BUG-154 | High | MITIGATED (DEC-037) | Employee / Person Address / Person Banking create-edit forms are id-based against the code-based schema |
| BUG-161 | High | OPEN (needs decision) | Bookings have no branch: xlr8_booking_master has no branch column and the enquiry fallback (dealer_branch) is empty on all 60,923 enquiries, so VOTF numbers ca… |
| BUG-168 | High | OPEN (booking team's code — reported, n… | Same route trap in the booking team's area: `accounts/receipt/{id}/show`, `sales/lead*` search/details/destroy, `sales/lead-source*`, `sales/enquiry/{id}` dest… |
| BUG-169 | High | CLOSED — accepted (DEC-046) | App timezone changed from UTC to Asia/Kolkata on origin/stage (booking team, 26-09-2026): timestamps written before are UTC, after are IST, in the same columns… |
